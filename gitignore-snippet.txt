# --- agent-harness-kit: paste into your project's .gitignore ---
# Version the shareable harness (.claude/settings.json, .claude/hooks/,
# .claude/commands/) so the repo IS your harness. Ignore only machine/session
# state and anything secret.
.claude/settings.local.json
.claude/projects/
.claude/todos/
.claude/shell-snapshots/
.claude/statsig/
.claude/.credentials.json
.claude/hooks/__pycache__/
# Resource broker: real config (creds/ports) is local; only the .example is versioned.
.claude/scripts/orchestration/broker.config.json
# Ephemeral per-worker worktrees created by the broker.
*-wt-[0-9]*/
# --- end snippet ---
